# ClinTrial Connect — ইনস্টলেশন ও টেস্টিং গাইড (বাংলা)

এই গাইডে ধাপে ধাপে দেখানো হয়েছে কীভাবে ClinTrial Connect প্রজেক্টটা আপনার নিজের কম্পিউটারে কপি করবেন,
ইনস্টল করবেন, চালাবেন, এবং automated + manual টেস্ট কেসগুলো এক্সিকিউট করবেন।

---

## ধাপ ০: যা যা আগে থেকে ইনস্টল থাকতে হবে (Prerequisites)

শুরু করার আগে আপনার কম্পিউটারে এই সফটওয়্যারগুলো ইনস্টল থাকতে হবে:

1. **Java Development Kit (JDK) 17 বা তার পরের ভার্সন** — Mac-এ Homebrew দিয়ে ইনস্টল থাকলে সাধারণত এমনিতেই
   17 বা তার বেশি থাকে (`brew install openjdk`)। নতুন করে দরকার হলে [https://adoptium.net](https://adoptium.net)
   থেকেও ডাউনলোড করতে পারেন।
   টার্মিনালে চেক করুন: `java -version`
2. **Apache Maven** — [https://maven.apache.org/download.cgi](https://maven.apache.org/download.cgi)
   টার্মিনালে চেক করুন: `mvn -version`
3. **MySQL Server 8.0+** — [https://dev.mysql.com/downloads/mysql/](https://dev.mysql.com/downloads/mysql/) থেকে
   ইনস্টল করুন (অথবা Windows/Mac হলে MySQL Installer/Homebrew ব্যবহার করুন)।
4. **(ঐচ্ছিক)** JMeter — লোড টেস্টিং করতে চাইলে: [https://jmeter.apache.org/download_jmeter.cgi](https://jmeter.apache.org/download_jmeter.cgi)
5. **Microsoft Excel বা Google Sheets** — ম্যানুয়াল টেস্ট কেস ফাইল (`ClinTrial_Connect_Test_Cases.xlsx`) খোলার জন্য।

---

## ধাপ ১: প্রজেক্ট কপি/এক্সট্র্যাক্ট করা

1. পাঠানো `clintrial-connect.zip` ফাইলটা আপনার কম্পিউটারে একটা সুবিধাজনক জায়গায় (যেমন `Documents` ফোল্ডারে)
   ডাউনলোড/সেভ করুন।
2. জিপ ফাইলটা এক্সট্র্যাক্ট (unzip) করুন — এতে `clintrial-connect` নামে একটা ফোল্ডার তৈরি হবে।
3. টার্মিনাল/কমান্ড প্রম্পট খুলে সেই ফোল্ডারে যান:
   ```bash
   cd path/to/clintrial-connect
   ```

---

## ধাপ ২: MySQL ডেটাবেজ তৈরি করা

1. MySQL সার্ভার চালু আছে কিনা নিশ্চিত করুন।
2. টার্মিনালে নিচের কমান্ডগুলো একে একে চালান (root পাসওয়ার্ড চাইলে দিন)। **লক্ষ্য করুন:** পাসওয়ার্ড হিসেবে
   `changeme`-এর বদলে একটা শক্তিশালী পাসওয়ার্ড ব্যবহার করা হয়েছে (কেন, তা নিচের ⚠️ নোটে ব্যাখ্যা করা আছে)।
   `IDENTIFIED WITH mysql_native_password` **ব্যবহার করা হয়নি ইচ্ছাকৃতভাবে** — নতুন MySQL ভার্সনে
   (8.4+/9.x) এই প্লাগইনটাই থাকে না (নিচের ⚠️ নোটে ব্যাখ্যা), তাই ডিফল্ট অথেন্টিকেশন প্লাগইনই ব্যবহার করা
   হয়েছে, যা পুরনো ও নতুন — দুই ধরনের MySQL-এই কাজ করে:
   ```bash
   mysql -u root -p -e "CREATE DATABASE IF NOT EXISTS clintrial_connect CHARACTER SET utf8mb4;"
   mysql -u root -p -e "DROP USER IF EXISTS 'clintrial_app'@'%';"
   mysql -u root -p -e "CREATE USER 'clintrial_app'@'%' IDENTIFIED BY 'ClinTrialQA@2026';"
   mysql -u root -p -e "GRANT ALL PRIVILEGES ON clintrial_connect.* TO 'clintrial_app'@'%';"
   mysql -u root -p -e "FLUSH PRIVILEGES;"
   ```
   (`DROP USER IF EXISTS` লাইনটা নিরাপদে বারবার চালানো যায় — আগের কোনো চেষ্টায় আধা-তৈরি ইউজার থেকে গেলে
   সেটা মুছে ফ্রেশভাবে আবার তৈরি করে।)
3. এবার টেবিল স্ট্রাকচার (স্কিমা) লোড করুন:
   ```bash
   mysql -u clintrial_app -p clintrial_connect < src/main/resources/schema-mysql.sql
   ```
   পাসওয়ার্ড চাইলে `ClinTrialQA@2026` দিন (অথবা আপনি উপরের কমান্ডে যে পাসওয়ার্ড বসিয়েছেন সেটাই দিন — শুধু
   পরের ধাপেও ঠিক একই পাসওয়ার্ড ব্যবহার করতে হবে)।

   > ⚠️ **`ERROR 1524 (HY000): Plugin 'mysql_native_password' is not loaded` পেলে:**
   > আপনার MySQL ভার্সন 8.4 বা তার পরের — এই ভার্সনগুলোতে `mysql_native_password` প্লাগইন ডিফল্টভাবে বন্ধ
   > (বা একদম নেই)। উপরের কমান্ডে এখন `IDENTIFIED WITH mysql_native_password` **নেই**, শুধু
   > `IDENTIFIED BY` — তাই এই এররটা আর আসার কথা না। যদি পুরনো কোনো নোট/স্ক্রিনশট অনুসরণ করে থাকেন, উপরের
   > হালনাগাদ কমান্ডগুলো ব্যবহার করুন।
   >
   > **`ERROR 1007`: `Can't create database 'clintrial_connect'; database exists`** — এটা আসলে সমস্যা
   > না, ডেটাবেজটা আগের কোনো চেষ্টায় ইতিমধ্যে তৈরি হয়ে গেছে বোঝায়। উপরের কমান্ডে এখন `CREATE DATABASE IF
   > NOT EXISTS` ব্যবহার করা হয়েছে, তাই এটা বারবার নিরাপদে চালানো যায়, কোনো এরর ছাড়াই।
   >
   > **`ERROR 1819` (password does not satisfy the current policy requirements) পেলে:**
   > MySQL-এর ডিফল্ট পাসওয়ার্ড পলিসি অনুযায়ী পাসওয়ার্ডে বড়হাতের অক্ষর, ছোটহাতের অক্ষর, সংখ্যা ও একটা
   > স্পেশাল ক্যারেক্টার (যেমন `#`, `!`) থাকতে হয় এবং কমপক্ষে ৮ অক্ষরের হতে হয় — `changeme` এই শর্ত পূরণ
   > করে না, তাই এরর দেয়। সমাধান হলো উপরের মতো একটা শক্তিশালী পাসওয়ার্ড (যেমন `ClinTrialQA@2026`) ব্যবহার
   > করা। এর ফলে **পরের `CREATE USER` কমান্ডটা ব্যর্থ** হওয়ায়, তার ঠিক পরের `GRANT` কমান্ডটাও
   > **`ERROR 1410` (You are not allowed to create a user with GRANT)** দিয়ে ব্যর্থ হয় — কারণ যে
   > ইউজারকে অনুমতি দেওয়ার কথা, সেটাই তৈরি হয়নি। তাই `CREATE USER` কমান্ডটা সফল হলেই `GRANT`
   > কমান্ডটাও এমনিতেই ঠিকমতো চলবে।

---

## ধাপ ৩: অ্যাপ্লিকেশন চালু করা (Run)

একই টার্মিনালে, প্রজেক্ট ফোল্ডারের ভেতরে থেকে:

```bash
export DB_HOST=localhost
export DB_PORT=3306
export DB_NAME=clintrial_connect
export DB_USER=clintrial_app
export DB_PASSWORD="ClinTrialQA@2026"

mvn spring-boot:run -Dspring-boot.run.profiles=mysql,demo
```

> Windows-এ PowerShell ব্যবহার করলে `export` এর বদলে `$env:DB_HOST="localhost"` এভাবে লিখতে হবে।

প্রথমবার চালানোর সময় Maven প্রয়োজনীয় লাইব্রেরিগুলো ইন্টারনেট থেকে ডাউনলোড করবে (একটু সময় লাগতে পারে)।
টার্মিনালে `Started ClinTrialConnectApplication` — এই লেখাটা দেখা গেলে বুঝবেন অ্যাপ চালু হয়ে গেছে।

`demo` প্রোফাইল দেওয়ায় স্বয়ংক্রিয়ভাবে কিছু নমুনা অ্যাকাউন্ট ও ট্রায়াল তৈরি হয়ে যাবে, যাতে সাথে সাথে টেস্ট করা যায়।

---

## ধাপ ৪: ব্রাউজারে অ্যাপ দেখা

ব্রাউজারে যান: **http://localhost:8080**

নমুনা (demo) লগইন — সবগুলোর পাসওয়ার্ড `Password123!`:

| ভূমিকা (Role) | ইমেইল |
|---|---|
| Admin | admin@clintrial.test |
| Physician (verified) | dr.patel@clintrial.test |
| Patient | patient1@clintrial.test |

এই অ্যাকাউন্টগুলো দিয়ে লগইন করে patient registration, trial browsing, matching, physician review, admin
dashboard — সব ফিচার ম্যানুয়ালি ঘুরে দেখতে পারবেন।

---

## ধাপ ৫: Automated টেস্ট চালানো

⚠️ **এই ধাপ শুরু করার আগে দুইটা জিনিস অবশ্যই করুন — না করলে টেস্টের বিশাল অংশ ফেইল করবে:**

1. **অ্যাপ নতুন করে (rebuild করে) চালু করুন।** এই রাউন্ডে `login`, `register`, `contact`, trial
   তৈরি/status-পরিবর্তন, physician verify, match approve/reject, আর logout — এই সবগুলো ফর্মে একটা
   আসল বাগ ঠিক করা হয়েছে (নিচে বিস্তারিত), তাই পুরনো (আগে থেকে চলমান) অ্যাপ প্রসেসটা বন্ধ করে আবার
   `mvn spring-boot:run -Dspring-boot.run.profiles=mysql,demo` দিয়ে চালু করতে হবে যাতে ফিক্সগুলো
   কার্যকর হয়।
2. **যে টার্মিনালে টেস্ট চালাবেন, সেখানেও `DB_HOST`/`DB_PORT`/`DB_NAME`/`DB_USER`/`DB_PASSWORD`
   এক্সপোর্ট করুন** (ধাপ ৩-এর কমান্ডটা), এমনকি অ্যাপ চালানোর টার্মিনালে আগে থেকে করা থাকলেও —
   `export` শুধু যে টার্মিনালে চালানো হয় সেখানেই কাজ করে, নতুন টার্মিনাল ট্যাব/উইন্ডোতে এটা বহন হয় না।
   না করলে `dbintegrity` আর কিছু JDBC-ভিত্তিক টেস্ট (AUTH-10, SEC-04, ADMIN-01 ইত্যাদি)
   `Access denied for user 'clintrial_app'@'localhost' (using password: YES)` এরর দিয়ে ফেইল করবে।

**⚠️ এই রাউন্ডে যে বাগটা ঠিক হয়েছে (CSRF token missing):** এই অ্যাপে CSRF protection ইচ্ছাকৃতভাবে
চালু রাখা হয়েছে (নিরাপত্তার জন্য দরকারি, `SecurityAutomationTest`-এর SEC-* কেসগুলো এটাই যাচাই করে)।
কিন্তু Thymeleaf ডিফল্টভাবে প্রতিটা ফর্মে CSRF hidden field নিজে থেকে বসিয়ে দেয় না — এর জন্য একটা
আলাদা Java bean রেজিস্টার করা লাগে, যেটা এই প্রজেক্টে করা ছিল না। ফলে প্রতিটা POST ফর্ম (লগইন,
রেজিস্ট্রেশন, কনট্যাক্ট, ট্রায়াল তৈরি, ম্যাচ approve/reject, physician verify, ট্রায়াল status বদল,
লগ-আউট) সাবমিট করলেই Spring Security সেটাকে ৪০৩ (Forbidden) দিয়ে বাতিল করে দিচ্ছিল — এটাই আগের বিশাল
টেস্ট-ফেইলিওর লিস্টের মূল কারণ ছিল। এখন প্রতিটা ফর্মে explicit করে CSRF token বসিয়ে দেওয়া হয়েছে, তাই
নতুন করে বিল্ড করে চালালে এই সমস্যাটা আর হবে না।

অ্যাপ বন্ধ করে (Ctrl+C দিয়ে) নতুন করে টার্মিনালে এই কমান্ডগুলো চালান:

```bash
# সব unit + integration + security টেস্ট একসাথে (MySQL লাগবে না, ভেতরে ভেতরে H2 ব্যবহার করে)
mvn test

# শুধু matching algorithm-এর টেস্ট
mvn -Dtest=MatchingServiceTest test

# শুধু role-based access / security টেস্ট
mvn -Dtest=RoleAccessSecurityTest test
```

টেস্ট শেষে `BUILD SUCCESS` লেখা দেখলে বুঝবেন সব টেস্ট পাস করেছে।

### Selenium (ব্রাউজার) টেস্ট চালানো (ঐচ্ছিক)

এর জন্য অ্যাপ আগে থেকে চালু থাকতে হবে (ধাপ ৩) এবং কম্পিউটারে Chrome ব্রাউজার ইনস্টল থাকতে হবে:

```bash
mvn -Dtest="selenium.**" -DfailIfNoTests=false -Dbase.url=http://localhost:8080 test
```

⚠️ **zsh ব্যবহারকারীদের জন্য নোট (Mac-এ ডিফল্ট শেল):** কমান্ডে `**` চিহ্নটা অবশ্যই উপরের মতো ডাবল-কোটের
(`"selenium.**"`) ভেতরে রাখবেন। কোট ছাড়া লিখলে zsh নিজে থেকে `**`-কে ফাইল-খোঁজার প্যাটার্ন হিসেবে ধরে
ফেলে, আর সেই প্যাটার্নে কোনো ফাইল/ফোল্ডার না পেলে এই এরর দেখায়:
```
zsh: no matches found: -Dtest=selenium.**
```
এটা Maven-এর সমস্যা না, zsh-এর নিজস্ব "globbing" আচরণ — কোট দিয়ে দিলেই ঠিক হয়ে যায়।

**xlsx-এর সবগুলো Test Case ID-এর জন্য লেখা automation suite** (Page Object Model + `automation`
প্যাকেজ) এই একই কমান্ডে চলবে, যেহেতু ওগুলোও `selenium` প্যাকেজের ভেতরেই আছে। কোন Test Case ID কোন
automated টেস্ট মেথডে আছে তার সম্পূর্ণ তালিকা `README-AUTOMATION.md` ফাইলে পাবেন।

### Database Integrity টেস্ট চালানো (ঐচ্ছিক)

DB-01 থেকে DB-04 পর্যন্ত টেস্ট কেসগুলো সরাসরি MySQL-এর constraint (CASCADE, unique, CHECK, foreign
key) যাচাই করে — এর জন্য অ্যাপ চালু থাকার দরকার নেই, শুধু `ধাপ ২`-এর MySQL ডেটাবেজ + `schema-mysql.sql`
লোড করা থাকলেই চলবে:

```bash
mvn -Dtest="dbintegrity.**" -DfailIfNoTests=false test
```

(এখানেও `**`-কে ডাবল-কোটের ভেতরে রাখতে হবে, একই zsh কারণে।)

### JMeter দিয়ে লোড টেস্ট (ঐচ্ছিক)

```bash
jmeter -n -t perf/clintrial-load-test.jmx -l results.jtl -e -o perf/report
```

ফলাফল `perf/report` ফোল্ডারে একটা HTML রিপোর্ট আকারে জমা হবে — ব্রাউজারে সেই ফোল্ডারের `index.html` খুলে দেখুন।

---

## ধাপ ৬: ম্যানুয়াল টেস্ট কেস ফাইল ব্যবহার করা

`ClinTrial_Connect_Test_Cases.xlsx` ফাইলটা Excel বা Google Sheets দিয়ে খুলুন। এতে যা আছে:

1. **Read Me ট্যাব** — পুরো টেস্ট স্যুটের স্কোপ ও ব্যাখ্যা।
2. **Summary ট্যাব** — কোন মডিউলে কতগুলো টেস্ট কেস আছে তার সারসংক্ষেপ।
3. বাকি ১১টা ট্যাব (Registration & Auth, Role-Based Access, Trial Management, Matching Algorithm,
   Physician Review, Admin Console, Contact & Notifications, Security, Performance & Load,
   Database Integrity, Responsive & A11y) — প্রতিটাতে Test Case ID, ধাপে ধাপে টেস্ট স্টেপ, প্রত্যাশিত
   ফলাফল, প্রায়োরিটি ও টাইপ দেওয়া আছে।

**যেভাবে ব্যবহার করবেন:**

- ধাপ ৩-এ চালু করা অ্যাপের বিপরীতে প্রতিটা টেস্ট কেস হাতে-কলমে এক্সিকিউট করুন (যেমন লগইন করে, ফর্ম পূরণ করে)।
- প্রতিটা সারিতে নিজে থেকে নতুন কলাম যোগ করে নিতে পারেন: **Actual Result**, **Status (Pass/Fail)**,
  **Tester**, **Date** — যাতে এক্সিকিউশনের ট্র্যাক রাখা যায়।
- চাইলে এই ফাইলটা TestRail, Xray, বা Zephyr-এর মতো কোনো টেস্ট-ম্যানেজমেন্ট টুলেও ইম্পোর্ট করতে পারবেন।

---

## সমস্যা হলে যা মনে রাখবেন

- **`mvn spring-boot:run` এরর দিচ্ছে?** — MySQL সার্ভার চালু আছে কিনা, আর ধাপ ২-এর ডেটাবেজ/ইউজার/পাসওয়ার্ড
  ঠিকভাবে সেট করা হয়েছে কিনা যাচাই করুন।
- **`ERROR 1819` বা `ERROR 1410` পাচ্ছেন?** — ধাপ ২-এর ⚠️ নোটে ব্যাখ্যা করা আছে; সংক্ষেপে, পাসওয়ার্ড
  MySQL-এর পলিসি অনুযায়ী যথেষ্ট শক্তিশালী নয়। বর্তমান পলিসি লেভেল দেখতে চাইলে: `mysql -u root -p -e
  "SHOW VARIABLES LIKE 'validate_password%';"`
- **`Access denied for user 'clintrial_app'@'localhost' (using password: YES)` পাচ্ছেন, অথচ CLI দিয়ে
  স্কিমা লোড করার সময় একই ইউজার/পাসওয়ার্ড দিয়ে ঠিকমতো কানেক্ট হয়েছিল?** — এটা MySQL 8-এর একটা পরিচিত
  সমস্যা: ডিফল্টভাবে MySQL `caching_sha2_password` নামের একটা authentication পদ্ধতি ব্যবহার করে, যেটা
  কখনো কখনো Java JDBC ড্রাইভার দিয়ে TCP কানেকশনের (অ্যাপ যেভাবে কানেক্ট করে) সাথে ঝামেলা করে, যদিও
  কমান্ড-লাইন (`mysql` CLI, যেটা Unix socket ব্যবহার করে) দিয়ে ঠিকই কাজ করে।

  প্রথমে দেখুন `application-mysql.properties`-এ JDBC URL-এ `allowPublicKeyRetrieval=true&useSSL=false`
  আছে কিনা (থাকার কথা, ইতিমধ্যে যোগ করা আছে) — বেশিরভাগ ক্ষেত্রে এটা থাকলেই caching_sha2_password
  ঠিকমতো কাজ করে, আর কিছু লাগে না।

  তাও সমস্যা থাকলে, **আপনার MySQL ভার্সন 8.4-এর নিচে হলে** ইউজারটাকে `mysql_native_password`
  পদ্ধতিতে পরিবর্তন করে দেখতে পারেন:
  ```bash
  mysql -u root -p -e "ALTER USER 'clintrial_app'@'%' IDENTIFIED WITH mysql_native_password BY 'ClinTrialQA@2026';"
  mysql -u root -p -e "FLUSH PRIVILEGES;"
  ```
  ⚠️ **কিন্তু MySQL 8.4 বা তার পরের ভার্সনে এই কমান্ডটা `ERROR 1524: Plugin 'mysql_native_password' is
  not loaded` দিয়ে ব্যর্থ হবে** — কারণ প্লাগইনটা ওই ভার্সনগুলোতে ডিফল্টভাবে বন্ধ/অনুপস্থিত। ভার্সন
  যাচাই করতে: `mysql -u root -p -e "SELECT VERSION();"`। MySQL 8.4+ হলে এই ALTER USER কমান্ডটা এড়িয়ে
  যান — সমস্যাটা তখন সাধারণত অন্য কিছু (যেমন পাসওয়ার্ড ভুল, বা ইউজার আদৌ তৈরি হয়নি — `SELECT user, host
  FROM mysql.user WHERE user='clintrial_app';` দিয়ে যাচাই করুন)।

  যেভাবেই সমাধান হোক, এরপর সরাসরি TCP দিয়ে কানেকশনটা যাচাই করে নিন (অ্যাপ ঠিক এভাবেই কানেক্ট করবে):
  ```bash
  mysql -h 127.0.0.1 -P 3306 -u clintrial_app -p clintrial_connect
  ```
  পাসওয়ার্ড চাইলে `ClinTrialQA@2026` দিন — এটা কোনো এরর ছাড়া কানেক্ট হলে বুঝবেন সমস্যাটা সমাধান হয়ে গেছে,
  এবং `mvn spring-boot:run ...` আবার চালাতে পারেন।

- **`mvn spring-boot:run` চালাতে গিয়ে `error: release version 21 not supported` পাচ্ছেন?** — আপনার
  কম্পিউটারে ইনস্টল করা Java-এর ভার্সন (`java -version` দিয়ে চেক করুন) প্রজেক্টের `pom.xml`-এ চাওয়া
  ভার্সনের চেয়ে পুরনো। এই প্রজেক্টটা এখন Java 17+ টার্গেট করে তৈরি (যা Homebrew-এর ডিফল্ট `openjdk`-এর সাথে
  মিলে যায়), তাই `java -version` 17 বা তার বেশি দেখালে এই এরর আর আসার কথা না। এরপরও যদি Java 17-এর চেয়ে
  পুরনো কোনো ভার্সন দেখায়, তাহলে `brew install openjdk` দিয়ে নতুন ভার্সন ইনস্টল করুন এবং সেটাকে ডিফল্ট
  করুন (`brew info openjdk` কমান্ডের আউটপুটে `JAVA_HOME` সেট করার নির্দেশনা দেওয়া থাকে)।
- **`Web server failed to start. Port 8080 was already in use` এরর পাচ্ছেন?** — মানে আগে থেকেই অন্য একটা
  প্রোগ্রাম (সম্ভবত আগের একটা `mvn spring-boot:run`, যদি সেটা অন্য কোনো টার্মিনাল ট্যাবে চলমান থেকে থাকে,
  বা অন্য কোনো অ্যাপ) ৮০৮০ পোর্টটা দখল করে বসে আছে। দুইভাবে ঠিক করতে পারেন:
  1. **যেটা পোর্ট দখল করে আছে সেটা বন্ধ করুন:**
     ```bash
     lsof -nP -iTCP:8080 -sTCP:LISTEN
     ```
     এই কমান্ড যে প্রসেসটা পোর্ট ব্যবহার করছে তার একটা `PID` (নম্বর) দেখাবে। তারপর:
     ```bash
     kill -9 <PID>
     ```
     (`<PID>` জায়গায় উপরে পাওয়া নম্বরটা বসান।) এরপর আবার `mvn spring-boot:run ...` কমান্ডটা চালান।
  2. **অথবা অন্য একটা পোর্টে অ্যাপ চালান** (৮০৮০ যা-ই দখল করে থাকুক, ঘাঁটাতে না চাইলে):
     ```bash
     mvn spring-boot:run -Dspring-boot.run.profiles=mysql,demo -Dspring-boot.run.arguments=--server.port=8081
     ```
     তারপর ব্রাউজারে যাবেন **http://localhost:8081**।
- **প্রথমবার `mvn` কমান্ড চালাতে অনেক সময় লাগছে?** — এটা স্বাভাবিক; Maven প্রয়োজনীয় লাইব্রেরি ডাউনলোড করছে,
  একবার ডাউনলোড হয়ে গেলে পরের বার অনেক দ্রুত চলবে।
- **CSRF ফিক্সের পরও `RoleBasedAccessAutomationTest.rbac01`/`rbac02` বা `SecurityAutomationTest.sec07`
  (অ্যানোনিমাস ইউজারের জন্য protected URL-এ গেলে ৪০৩ পাওয়া, ৩০২ redirect-এর বদলে) ফেইল করছে?** —
  সততার সাথে বলছি: `SecurityConfig.java` কোড রিভিউ করে দেখা গেছে এখানে কোনো custom
  `AccessDeniedHandler`/`AuthenticationEntryPoint` নেই, তাই Spring Security-র স্বাভাবিক ডিফল্ট আচরণ
  অনুযায়ী অ্যানোনিমাস ইউজারের জন্য এটা ৩০২ redirect-ই দেওয়ার কথা — কোডে এমন কিছু পাওয়া যায়নি যেটা
  ইচ্ছাকৃতভাবে ৪০৩ ফেরত দেবে। তাই এটা সম্ভবত আগের রাউন্ডের CSRF-বাগের কারণে সৃষ্ট একটা পরোক্ষ (cascading)
  উপসর্গ ছিল। CSRF ফিক্স আর অ্যাপ rebuild+restart করার পর যদি এই তিনটা টেস্ট এখনো ৪০৩ দেখায়, সেটা নতুন ও
  আসল সিগন্যাল — তখন টার্মিনালের ফ্রেশ আউটপুট/stack trace শেয়ার করলে সেটা নিয়ে আরও গভীরে গিয়ে দেখা যাবে।
- আরও বিস্তারিত টেকনিক্যাল তথ্যের জন্য প্রজেক্টের ভেতরের `README.md` ফাইলটাও দেখতে পারেন (ইংরেজিতে লেখা)।
